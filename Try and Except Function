try:
 age = int(input("How old are you?"))
except ValueError:
 print("You have typed in a an invalid number, try to with numerical asnwer like '15'")
 age = int(input("How old are you?"))

if age > 18:
 print(f"You can drive at age {age}.")

# With try and except functionality you can catch the bug and give alternative path 
